# Deployment-only dependencies (Railway). torch is installed separately in
# the Dockerfile from the CPU-only wheel index - Railway has no GPU, and the
# default PyPI torch wheel pulls in ~2GB of unused CUDA libraries.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.0,<3.0
tokenizers>=0.20,<1.0
numpy>=1.26,<3.0
PyYAML>=6.0,<7.0
